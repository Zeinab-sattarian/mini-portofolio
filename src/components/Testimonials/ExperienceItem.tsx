interface ExperienceItemProps {
  title: string;
  company: string;
  startDate: string;
  finishDate: string;
  description: string;
}

const ExperienceItem = ({
  title,
  company,
  startDate,
  finishDate,
  description,
}: ExperienceItemProps) => {
  return <div></div>;
};
export default ExperienceItem;
